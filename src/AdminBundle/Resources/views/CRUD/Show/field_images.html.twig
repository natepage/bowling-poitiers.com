{% extends '@SonataAdmin/CRUD/base_show_field.html.twig' %}

{% block field %}
    {% for element in value %}
        <div class="col-md-4">
            <img src="{{ asset(element.webPath) }}" class="img-thumbnail" />
        </div>
    {% endfor %}
{% endblock field %}
